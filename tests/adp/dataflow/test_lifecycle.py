"""Dataflow lifecycle (destructive) acceptance tests.

Note: This file is currently empty. The following tests will be added when
CLI support becomes available:
- test_dataflow_create_and_delete (dataflow create / delete)
- test_dataflow_update (dataflow update)
- test_dataflow_full_lifecycle (create → validate → run → delete)

Requires: EVAL_RUN_DESTRUCTIVE=1 + database credentials.
Pattern: follows bkn/test_lifecycle.py conventions (try/finally cleanup).
"""

# Tests will be added here when CLI commands are implemented
