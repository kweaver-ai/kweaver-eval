"""Dataflow query and execution acceptance tests.

Note: This file is currently empty. The following tests will be added when
CLI support becomes available:
- test_dataflow_dry_run (dataflow run --dry-run)
- test_dataflow_preview_source (dataflow preview)
- test_dataflow_execute_single_node (dataflow run --node)
- test_dataflow_history (dataflow history)
- test_dataflow_schema_inference (dataflow schema)

Pattern: follows vega/test_query.py conventions.
"""

# Tests will be added here when CLI commands are implemented
